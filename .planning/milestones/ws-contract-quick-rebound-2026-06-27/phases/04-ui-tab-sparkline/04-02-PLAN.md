---
phase: 04-ui-tab-sparkline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/screens/rebound_dashboard_screen.dart
  - lib/screens/main_navigation.dart
  - lib/screens/kline_screen.dart
  - lib/widgets/kline_chart_widget.dart
  - lib/providers/rebound_score_provider.dart
  - lib/services/rebound/rebound_alert_service.dart
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "用户可以看到按周期分 Tab（15m/1h/4h/1d）的反弹看板"
    - "每个 Tab 内信号按评分降序排列，每行显示币种/周期/跌幅/回补%/评分/迷你 sparkline"
    - "死猫风险被图标/颜色标注，止损参考位可见"
    - "Warming-up 状态标的明确展示且不显示信号"
    - "全项目 grep 无 '买入'/'强买'（不含注释）"
    - "底部固定风险提示：回测需打 30-50% 折扣，不构成投资建议"
    - "点击信号行可下钻到 KlineScreen，携带 symbol + interval + 高亮窗口"
  artifacts:
    - path: "lib/screens/rebound_dashboard_screen.dart"
      provides: "反弹信号实时看板页面"
      min_lines: 300
    - path: "lib/screens/main_navigation.dart"
      provides: "底部导航栏（新增反弹 Tab）"
    - path: "lib/screens/kline_screen.dart"
      provides: "K线详情页（新增 highlight 标注参数）"
    - path: "lib/widgets/kline_chart_widget.dart"
      provides: "K线图组件（新增 highlight 标注支持）"
    - path: "lib/providers/rebound_score_provider.dart"
      provides: "信号状态 + 最近收盘价（sparkline 数据），per D-01"
    - path: "lib/services/rebound/rebound_alert_service.dart"
      provides: "编排器（传递最近收盘价到 Provider）"
  key_links:
    - from: "lib/screens/rebound_dashboard_screen.dart"
      to: "lib/providers/rebound_score_provider.dart"
      via: "Provider.of<ReboundScoreProvider>(context) — 消费 getSignalsForTimeframe(tf)"
      pattern: "Provider.of.*ReboundScoreProvider"
    - from: "lib/screens/rebound_dashboard_screen.dart"
      to: "lib/screens/kline_screen.dart"
      via: "Navigator.push(MaterialPageRoute(builder: (_) => KlineScreen(symbol: ..., ...)))"
      pattern: "Navigator.*push.*KlineScreen"
    - from: "lib/screens/main_navigation.dart"
      to: "lib/screens/rebound_dashboard_screen.dart"
      via: "IndexedStack 第 6 个 child + BottomNavigationBarItem"
      pattern: "ReboundDashboardScreen"
    - from: "lib/services/rebound/rebound_alert_service.dart"
      to: "lib/providers/rebound_score_provider.dart"
      via: "upsert 调用时传递最近收盘价列表"
      pattern: "_provider.upsert"
---

<objective>
创建「合约反弹监控」实时看板页面，按周期分 Tab（15m/1h/4h/1d），信号按评分降序排列，每行展示币种/周期/跌幅/回补%/评分/迷你 sparkline，标注死猫风险与止损参考位，点击信号下钻到 KlineScreen。全项目文案合规（无「买入/强买」等执行性措辞）。

Purpose：用户能在一个看板上跨周期监控所有反弹候选，按评分快速排序发现最值得关注的标的，并通过下钻验证信号合理性。看板独立消费 Phase 3 产出的 Provider 状态，与 Phase 5（推送提醒）并行不冲突。

Output：可交互的反弹看板页面 + 导航集成 + KlineScreen 下钻路由 + sparkline 数据流
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/providers/rebound_score_provider.dart
@lib/models/rebound_signal.dart
@lib/screens/kline_screen.dart
@lib/widgets/kline_chart_widget.dart
@lib/screens/main_navigation.dart
@lib/services/rebound/rebound_alert_service.dart
@lib/services/rebound/rebound_kline_stream_service.dart
@lib/main.dart
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sparkline 数据流 —— Provider + AlertService 增强</name>
  <files>lib/providers/rebound_score_provider.dart, lib/services/rebound/rebound_alert_service.dart</files>
  <behavior>
    - 测试 1：provider.upsert(symbol, tf, signal, recentCloses: [99, 100, 101]) 后，provider.getRecentCloses(symbol, tf) 返回 [99, 100, 101]
    - 测试 2：不传 recentCloses 时 getRecentCloses 返回 null（向后兼容）
    - 测试 3：多次 upsert 同一 symbol+tf，最近收盘价被最新值覆盖
  </behavior>
  <action>
为迷你 sparkline 渲染准备数据流。修改两个文件（最小侵入，保持向后兼容）：

**A) ReboundScoreProvider 增强（`lib/providers/rebound_score_provider.dart`）**

新增字段：
```dart
/// symbol → timeframe → 最近 N 根收盘价（用于 sparkline 渲染）
final Map<String, Map<String, List<double>>> _recentClosesBySymbol = {};
```

修改 `upsert` 方法签名，增加可选参数 `List<double>? recentCloses`：
```dart
void upsert(String symbol, String tf, ReboundSignal? signal, {List<double>? recentCloses})
```

当 `recentCloses != null` 时：
- `_recentClosesBySymbol.putIfAbsent(symbol, () => {});`
- `_recentClosesBySymbol[symbol]![tf] = recentCloses;`

新增只读查询方法：
```dart
/// 获取最近收盘价（用于 sparkline 渲染）。无数据返回 null。
List<double>? getRecentCloses(String symbol, String tf) =>
    _recentClosesBySymbol[symbol]?[tf];
```

`clear()` 和 `removeSymbol()` 方法也需要清理 `_recentClosesBySymbol` 中的对应数据。

**B) ReboundAlertService 传递收盘价（`lib/services/rebound/rebound_alert_service.dart`）**

在 `handleClosedKline` 方法的最后，拿窗口收盘价传给 provider：
```dart
// 提取最近最多 20 根收盘价（window 已排序，取最后 20 根）
final closes = window.length > 20
    ? window.sublist(window.length - 20).map((k) => k.close).toList()
    : window.map((k) => k.close).toList();
_provider.upsert(c.symbol, c.timeframe, signal != null ? enriched : null, recentCloses: closes);
```

注意：window 变量在 handleClosedKline 的步骤 2 中已获取，需要在最终 upsert 调用处仍可及。如当前代码结构在步骤 3-6 之间 window 可能被移动/覆盖，确保在方法末尾仍能引用 window（或不需移动，直接在两个 upsert 调用处都加上 recentCloses）。

**向后兼容验证：** 既有测试（rebound_score_provider_test.dart、rebound_alert_service_test.dart）仍全通过。

不要修改 Phase 2 ReboundDetector 或 ReboundConfluenceScorer——sparkline 是纯 UI 消费端功能。
</action>
<verify>
<automated>flutter test test/providers/rebound_score_provider_test.dart test/services/rebound/rebound_alert_service_test.dart 2>&1</automated>
</verify>
<done>既有 9 个测试全通过；provider.upsert 接受可选 recentCloses 参数；getRecentCloses 返回最近收盘价或 null</done>
</task>

<task type="auto">
  <name>Task 2: ReboundDashboardScreen —— 看板主体（TabBar + 信号列表 + sparkline + 死猫风险 + warm-up + 风险提示）</name>
  <files>lib/screens/rebound_dashboard_screen.dart</files>
  <action>
创建 `lib/screens/rebound_dashboard_screen.dart`（Flutter StatefulWidget with SingleTickerProviderStateMixin），per D-03/D-04/D-05。中文 UI 文案（per CLAUDE.md）。

**文件结构（`lib/screens/rebound_dashboard_screen.dart`）：**

```
// imports: flutter/material, provider, tomapp/models/rebound_signal, 
//         tomapp/providers/rebound_score_provider, fl_chart/fl_chart

/// 反弹监控看板页面
class ReboundDashboardScreen extends StatefulWidget { ... }
class _ReboundDashboardScreenState extends State<ReboundDashboardScreen>
    with SingleTickerProviderStateMixin { ... }

// 私有子组件（提取为独立 StatelessWidget 或方法）：
class _SignalRow extends StatelessWidget { ... }        // 单行信号
class _MiniSparkline extends StatelessWidget { ... }    // 迷你 sparkline
class _DeadCatIndicator extends StatelessWidget { ... } // 死猫风险标注
```

**A) TabBar（per D-03）：**

用 `TabController(length: 4)` + `TabBar` + `TabBarView`：
- Tab 标签：`15m`、`1h`、`4h`、`1d`
- 从 ReboundScoreProvider 读取信号：`Provider.of<ReboundScoreProvider>(context)`
- 调用 `provider.getSignalsForTimeframe(tf)` 按 score 降序获取信号列表

**B) 信号行布局（_SignalRow，per D-04/DASH-02）：**

每行一个 `Card` 或 `ListTile`，从左到右排列：
1. **币种**（`signal.symbol.replaceAll('USDT', '')`，加粗字体）
2. **评分**（`signal.score`，圆形带背景颜色：`score>=70` 深绿、`score>=40` 黄/橙、`score<40` 灰）
3. **跌幅**（`'${signal.dropMagnitude.toStringAsFixed(1)}' × ATR`，文字显示）
4. **回补%**（`'${(signal.recoveryRatio * 100).toStringAsFixed(0)}%'`）
5. **死猫风险**（_DeadCatIndicator，per D-05）：根据 `signal.deadCatRiskScore` 显示图标/颜色——`>=70` 红色骷髅图标 + "高风险" 文字，`>=40` 橙色警告图标 + "注意"，`<40` 绿色勾号或透明
6. **止损参考位**（`'${signal.swingLowPrice.toStringAsFixed(3)}'`，灰色小字标注 "止损"）
7. **迷你 sparkline**（_MiniSparkline）：从 `provider.getRecentCloses(signal.symbol, signal.timeframe)` 获取数据。如 null 或空则显示空白占位。

**C) 迷你 sparkline（_MiniSparkline，per D-04）：**

使用 fl_chart 1.2 `LineChart` 渲染迷你折线图（SparklineData）：
- 尺寸：宽度 `MediaQuery.of(context).size.width * 0.25`，高度 40
- 无轴标签、无网格、无边框（干净的迷你图）
- 最后一点价格高于第一点：绿色线；否则：红色线
- 下跌段区域（dropStartIndex 到 dropEndIndex）用半透明红色填充背景
- 拉回段区域（dropEndIndex 到 recoveryEndIndex）用半透明绿色填充背景
- 数据不足 2 点时显示空占位 `SizedBox(width: 60, height: 40)`
- 使用 fl_chart 1.2 `LineChartData` API（follow Plan 04-01 迁移后的类型系统）

fl_chart 1.2 关键类型（sparkline 用）：
- `LineChart(LineChartData(...))`
- `LineChartBarData(spots: [...], ...)` —— 线数据
- `FlSpot(x, y)` —— 数据点
- `GridData(show: false)` / `BorderData(show: false)` / `TitlesData(show: false)`

sparkline 坐标映射：
- x 轴：0..closes.length-1（数据点索引）
- y 轴：closes 的值，`minY` 设 closes.min * 0.995，`maxY` 设 closes.max * 1.005

**D) Warm-up 状态（per D-06/DASH-04）：**

在 TabBarView 顶部（信号列表之前）显示 warming-up 标的数量：
```dart
// 需要从 ReboundKlineStreamService 获取 warming-up 状态
// 如 provider 无法直接获取，可在 provider 中新增 warmingUpSymbols getter
```

如果 ReboundScoreProvider 当前不暴露 warming-up 状态，则从 `ReboundKlineStreamService` 获取（通过 Provider 查找或构造函数注入）。最简方案：在 ReboundScoreProvider 中新增 `Set<String> get warmingUpSymbols` 和对应的 setter，由 ReboundAlertService 定期更新。

Warm-up 显示文案："监控准备中 · {count} 个合约数据加载中"（淡灰色文字，位于列表顶部）。

**E) 风险提示（底部固定，per D-07）：**

在页面底部用固定定位的 Container 显示：
```
历史回测需打 30-50% 折扣，不构成投资建议
```
浅灰色背景、小号字体（12sp）、居中，始终可见（不随列表滚动）。

**F) 文案禁止词（per D-07/DASH-05）：**

所有 UI 文本中不使用「买入」「强买」「信号」「发财」「保证」「盈利」等执行性/诱导性措辞。信号集合统一称为「监控候选」而非「信号」。拒绝 "信号" 字样在 UI 中出现——如果需要指代检测输出，使用「监控候选」或直接描述（如"X 个候选"）。

<!-- planner-discipline-allow: 买入 -->
<!-- planner-discipline-allow: 强买 -->
<!-- planner-discipline-allow: 信号 -->

Not using 买入/强买 in UI text——these allow-marks are EXPLICITLY placed so the commit-grep (which will search for those Chinese words in code, not comments) does NOT match false positives from this plan documentation. The actual Dart strings in the source file MUST NOT contain those three words.

**G) StatefulWidget 生命周期：**

在 `initState` 中通过 `WidgetsBinding.instance.addPostFrameCallback` 延迟调用 ReboundAlertService.start()（per Phase 3 SUMMARY 设计：编排器由 Phase 4 看板按需启动，避免 app 启动时建 1600 路 WS）。
- 从某处获取初始 symbol 列表（Top 50 USDT 永续或从 ExchangeInfoService 获取）。如果获取 symbol 列表的逻辑复杂，先硬编码 BTCUSDT + ETHUSDT + Top 10 作为 MVP。
- 在 `dispose` 中调用 `ReboundAlertService.stop()`（通过 Provider 获取）。

**H) Auto-start ReboundAlertService：**

看板需要 ReboundAlertService 实例来启动 WS 流。Phase 3 的 ReboundAlertService 未在 Provider 树中注册。在看板 initState 中创建并持有：
```dart
late ReboundAlertService _alertService;

@override
void initState() {
  super.initState();
  final streamService = ReboundKlineStreamService();
  final detector = ReboundDetector();
  final provider = context.read<ReboundScoreProvider>();
  _alertService = ReboundAlertService(
    streamService: streamService,
    detector: detector,
    provider: provider,
  );
  WidgetsBinding.instance.addPostFrameCallback((_) async {
    // 获取 symbol 列表并启动
    final symbols = await _loadInitialSymbols(); // 从 ExchangeInfoService 获取 Top N
    await _alertService.start(symbols);
  });
}
```

注意：需要 import ReboundAlertService、ReboundKlineStreamService、ReboundDetector、ExchangeInfoService。

如 ExchangeInfoService 获取合约列表涉及异步初始化问题，简化方案：在 Provider 内预置 Top 20 USDT 永续列表（如 BTC, ETH, BNB, SOL, XRP, DOGE, ADA, AVAX, DOT, LINK, MATIC, UNI, ATOM, LTC, FIL, APT, ARB, OP, NEAR, TIA）——这些是交易所流动性最好的标的，可能覆盖 90% 的交易需求。用 provider 提供的静态方法或直接写死在 initState 中。

**空状态处理：**
- 无信号时：TabBarView 内显示 "暂无监控候选" 居中文字
- 某 Tab 无信号：该 Tab 内容区显示空状态
- 整个看板无数据：全页居中显示 "正在加载市场数据..."

</action>
<verify>
<automated>flutter analyze lib/screens/rebound_dashboard_screen.dart 2>&1 | grep -c 'No issues found\|no issues'</automated>
</verify>
<done>
ReboundDashboardScreen 编译通过；4 个 Tab 可切换；信号按 score 降序；每行展示币种/周期/跌幅/回补%/评分/sparkline；死猫风险图标/颜色正确；warm-up 状态显示；底部风险提示固定可见
</done>
</task>

<task type="auto">
  <name>Task 3: MainNavigation 集成 + KlineScreen 下钻路由 + 全项目文案合规 grep</name>
  <files>lib/screens/main_navigation.dart, lib/screens/kline_screen.dart, lib/widgets/kline_chart_widget.dart</files>
  <action>
**A) MainNavigation 集成（per D-08）：**

在 `lib/screens/main_navigation.dart` 中新增第 6 个底部导航 Tab——「反弹」。

修改步骤：
1. 新增 import：`import 'rebound_dashboard_screen.dart';`
2. `_screens` 列表添加第 6 项：`const ReboundDashboardScreen()`
3. `items` 列表添加 Tab 项：
   ```dart
   BottomNavigationBarItem(
     icon: Icon(Icons.replay_circle_filled_outlined ?? Icons.replay_outlined),
     activeIcon: Icon(Icons.replay_circle_filled ?? Icons.replay),
     label: '反弹',
   ),
   ```
   注意：`Icons.replay_circle_filled` 在 Material Icons 中可能不存在，使用 `Icons.replay` 或 `Icons.trending_up`。检查 Flutter SDK 提供的图标——`Icons.trending_up` 表示上升趋势，更贴切反弹语义。

使用 `Icons.trending_up` for active tab 和 `Icons.trending_up_outlined` for inactive tab（如 Flutter 版本不支持 outlined 变体，退回用 `Icons.trending_up` 或 `Icons.show_chart`）。

**B) KlineScreen 下钻路由（per D-08/DASH-06）：**

修改 `lib/screens/kline_screen.dart` 以支持高亮窗口标注：

1. 新增可选参数：
   ```dart
   class KlineScreen extends StatefulWidget {
     final String symbol;
     final String? defaultInterval;
     final int? highlightStartMs;  // 下跌段起始时间（毫秒时间戳）
     final int? highlightEndMs;    // 拉回段结束时间（毫秒时间戳）
   ```

2. 将 `highlightStartMs` 和 `highlightEndMs` 传递给 `_KlineChartWidget`（或直接放在 KlineScreen 的 Column children 中作为 overlay）。

3. 修改 `lib/widgets/kline_chart_widget.dart`：
   - 新增可选字段 `final int? highlightStartMs; final int? highlightEndMs;`
   - 如两者均非 null，在 chart 上方叠加半透明高亮区域：
     ```dart
     // 在 build() 中用 Stack 包裹 KChartWidget：
     Stack(
       children: [
         KChartWidget(...),  // 原有图表
         if (highlightStartMs != null && highlightEndMs != null)
           Positioned.fill(
             child: IgnorePointer(
               child: CustomPaint(
                 painter: _HighlightPainter(
                   startMs: highlightStartMs!,
                   endMs: highlightEndMs!,
                   data: klineData,
                 ),
               ),
             ),
           ),
       ],
     )
     ```

4. 创建 `_HighlightPainter`（CustomPainter）：
   - 根据数据列表找到 startMs/endMs 对应的 K 线索引位置
   - 计算该区域在图表中的 x 范围（按数据索引占总数据长度的比例映射到 chart 宽度）
   - 绘制半透明矩形：`下跌段`用红色半透明（`Colors.red.withOpacity(0.15)`），`拉回段`用绿色半透明（`Colors.green.withOpacity(0.15)`）
   - 如找不到对应索引（时间不匹配），跳过绘制

5. 可在 `_SignalRow.onTap` 中导航到 KlineScreen：
   ```dart
   Navigator.of(context).push(MaterialPageRoute(
     builder: (_) => KlineScreen(
       symbol: signal.symbol,
       defaultInterval: signal.timeframe,
       highlightStartMs: signal.timestamp.millisecondsSinceEpoch - 
           ((signal.recoveryEndIndex - signal.dropStartIndex) * tfDurationMs).toInt(),
       highlightEndMs: signal.timestamp.millisecondsSinceEpoch,
     ),
   ));
   ```
   注意：上述时间估算不精确。从 ReboundSignal 中可获取 `dropStartIndex`、`dropEndIndex`、`recoveryEndIndex` 和 `timestamp`（确认 K 线时间）。通过 `timestamp`（确认 K 线时间）和各段的 K 线数（`dropEndIndex - dropStartIndex + 1` / `recoveryEndIndex - dropEndIndex + 1`）乘以周期时长（如 15m=900000ms）反推起始时间戳。此逻辑放在 _SignalRow 的 onTap 回调中。

   tfDurationMs 映射：15m = 900000, 1h = 3600000, 4h = 14400000, 1d = 86400000。

   简化方案：如果时间戳计算太复杂，可将 highlightStartMs/highlightEndMs 设为 null 先交付基本下钻，标 TODO 待后续细化。但尽量实现——D-08 明确要求 "高亮检测到的下跌+拉回窗口"。

**C) 全项目文案合规 grep（per D-07/DASH-05）：**

在修改的所有新 Dart 文件中，验证无禁止词：
```bash
# 检查所有本次修改或新建的 Dart 文件中是否包含禁止词
# （用 grep -v '^#' 过滤注释行后再检查——避免 plan 文档自身的 allow 标记误触发）
grep -n '买入\|强买' lib/screens/rebound_dashboard_screen.dart lib/screens/main_navigation.dart lib/widgets/kline_chart_widget.dart 2>/dev/null; echo "EXIT: $?"
```

构建前确认：新文件中的 UI 字符串使用「监控候选」而非单独出现的「信号」（指代检测输出时用「监控候选」或「X 个候选」）。

允许「信号」一词出现在注释/文档/import 路径/类名（如 `ReboundSignal`）和 provider 方法名（`getSignalsForTimeframe`）中——禁止的是面向用户的 UI 文案。

**D) 路由注册确认：**

ReboundDashboardScreen 不通过命名路由，直接由 `MainNavigation` 的 `IndexedStack` 持有。底部导航点击 "反弹" Tab 时 `IndexedStack` 切换到对应 `_currentIndex`（索引 5）。

确认 `_currentIndex` 初始值为 0（首页），用户可点击切换。不需要修改路由表（无命名路由注册）。
</action>
<verify>
<automated>
# 1. 编译检查
flutter analyze lib/screens/rebound_dashboard_screen.dart lib/screens/main_navigation.dart lib/screens/kline_screen.dart lib/widgets/kline_chart_widget.dart 2>&1

# 2. UI 文案禁止词检查（检查所有 Phase 4 修改/新建的文件中不含禁止词在 UI 字符串中）
# grep 过滤: 排除 import/注释行中的类名引用，只检查字符串字面量和文本 widget
</automated>
</verify>
<done>
- flutter analyze 0 错误
- MainNavigation 有 6 个 Tab（含 "反弹"），点击切换到 ReboundDashboardScreen
- 点击信号行 push KlineScreen，传递正确的 symbol 和 defaultInterval
- KlineScreen 高亮下跌+拉回窗口（如有 highlightStartMs/EndMs）
- 新文件 grep 无 UI 字符串「买入」「强买」（不含代码注释和类名）
- 底部固定风险提示始终可见
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → 看板 UI | 用户点击信号行触发下钻导航——标准 Flutter Navigator，无注入点 |
| Provider → 看板 UI | ReboundScoreProvider 数据通过 ChangeNotifier 传递——Provider 位于同一进程内，无序列化边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-04-03 | Tampering | ReboundScoreProvider.upsert recentCloses param | accept | recentCloses 仅用于 UI sparkline 渲染，不影响检测逻辑。恶意数据只能在 UI 层显示错误 sparkline，不触发交易行为。与检测器纯函数隔离。 |
| T-04-04 | Information Disclosure | Navigator.push KlineScreen | accept | KlineScreen 是既有页面，数据来自 Binance 公开 API。signal 参数不包含敏感信息。 |
| T-04-05 | Elevation of Privilege | ReboundAlertService.start() | accept | 编排器由看板 initState 启动，与 Phase 3 已有的 ReboundAlertService 代码路径一致。无新增权限边界。 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本阶段无 npm/pip/cargo 包操作 |
</threat_model>

<verification>
## 整体验证

```bash
# 1. 全项目编译
flutter analyze

# 2. 既有测试回归（确保 Provider/AlertService 改动未破坏 Phase 3 测试）
flutter test test/providers/rebound_score_provider_test.dart test/services/rebound/rebound_alert_service_test.dart

# 3. 文案合规——UI 文件中的字符串不含禁止词
# 注意：不 grep 注释行（grep -v '//'），只看实际 UI 字符串
grep -nE '买入|强买' lib/screens/rebound_dashboard_screen.dart 2>/dev/null || echo "PASS"
```
</verification>

<success_criteria>
- [ ] ReboundDashboardScreen 编译通过，flutter analyze 0 错误
- [ ] 4 个周期 Tab（15m/1h/4h/1d）可切换
- [ ] 每个 Tab 信号按 score 降序排列
- [ ] 每行显示币种/周期/跌幅/回补%/评分/迷你 sparkline
- [ ] 死猫风险标注（图标/颜色）+ 止损参考位可见
- [ ] Warming-up 状态显示且不出信号
- [ ] 底部固定风险提示："历史回测需打 30-50% 折扣，不构成投资建议"
- [ ] 点击信号下钻到 KlineScreen（symbol + interval + highlight 窗口）
- [ ] MainNavigation 新增 "反弹" Tab（第 6 个）
- [ ] 新文件 grep 无 UI 文案「买入」「强买」
- [ ] Phase 3 既有 provider/alert_service 测试全通过（无回归）
</success_criteria>

<output>
Create `.planning/workstreams/contract-quick-rebound/phases/04-ui-tab-sparkline/04-02-SUMMARY.md` when done
</output>
