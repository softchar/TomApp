# 币安资金费率智能监控服务 - 需求文档

## 项目概述

### 项目名称
币安资金费率智能监控服务

### 项目描述
一个基于 Flutter 开发的智能监控应用，用于动态监控币安合约市场的资金费率变化，支持 Android/iOS 移动端，使用本地数据库存储数据。

### 技术栈
- **Flutter** - 跨平台移动应用框架
- **Dart** - 编程语言
- **sqflite** - SQLite 本地数据库
- **http** - HTTP 请求库
- **provider** - 状态管理
- **flutter_local_notifications** - 本地通知

---

## 功能需求

### 1. 资金费率监控服务

#### 1.1 智能监控频率
- **常规模式**: 每 10 秒获取一次资金费率
- **高频模式**: 整点时段（整点前 1 分钟到整点后 5 分钟）每 5 秒获取一次

#### 1.2 监控范围
- 支持所有币安期货合约
- 筛选资金费率计算间隔小于 4 小时的合约
- 按间隔时间从短到长排序，显示前 100 个合约

#### 1.3 数据展示
- 列表展示实时监控报告
- 显示合约名称、资金费率计算间隔
- 显示当前监控模式和总合约数

### 2. 数据持久化

#### 2.1 合约实体 (Contract)
| 字段 | 类型 | 描述 |
|------|------|------|
| id | int | 主键 |
| symbol | String | 交易对 |
| price | double | 当前价格 |
| fundingRate | double | 资金费率 |
| fundingRateInterval | int | 费率时间间隔 |
| isActive | bool | 是否激活 |
| createdAt | DateTime | 创建时间 |
| updatedAt | DateTime | 更新时间 |

#### 2.2 数据库操作
- 自动创建本地 SQLite 数据库
- 支持单个/批量保存合约
- 更新已存在合约的数据
- 查询所有合约（按更新时间排序）

### 3. 币安 API 集成

#### 3.1 API 端点
| 端点 | 描述 |
|------|------|
| `/fapi/v1/fundingRate` | 查询资金费率历史 |
| `/fapi/v1/premiumIndex` | 最新标记价格和资金费率 |
| `/fapi/v1/fundingInfo` | 获取资金费率信息 |
| `/fapi/v1/exchangeInfo` | 获取交易所信息 |

#### 3.2 基础 URL
- `https://fapi.binance.com`

---

## 项目架构

### 分层架构
```
lib/
├── main.dart                 # 应用入口
├── models/                   # 数据模型层
│   └── contract.dart        # 合约模型
├── services/                 # 服务层
│   ├── binance_api_service.dart      # 币安 API 服务
│   ├── funding_rate_monitor_service.dart  # 监控服务
│   ├── database_service.dart           # 数据库服务
│   └── notification_service.dart       # 通知服务
├── providers/                # 状态管理层
│   └── contract_provider.dart         # 合约状态管理
├── screens/                  # 页面层
│   ├── home_screen.dart      # 首页
│   └── contract_list_screen.dart   # 合约列表页
└── utils/                    # 工具类
    └── constants.dart        # 常量定义
```

### 核心类说明

| 类/接口 | 职责 |
|---------|------|
| `FundingRateMonitorService` | 后台监控服务 |
| `BinanceApiService` | 币安 API 调用 |
| `DatabaseService` | 数据库操作 |
| `ContractProvider` | 状态管理 |
| `NotificationService` | 本地通知 |

---

## 依赖包

| 包名 | 版本 | 用途 |
|------|------|------|
| sqflite | ^2.3.0 | SQLite 本地数据库 |
| http | ^1.1.0 | HTTP 请求 |
| provider | ^6.0.0 | 状态管理 |
| flutter_local_notifications | ^16.0.0 | 本地通知 |
| path | ^1.8.0 | 路径操作 |
| intl | ^0.18.0 | 国际化/日期格式化 |

---

## 页面设计

### 1. 首页 (HomeScreen)
- 显示当前监控状态（运行中/已停止）
- 显示当前模式（常规/高频）
- 显示监控的合约数量
- 启动/停止监控按钮
- 跳转到合约列表按钮

### 2. 合约列表页 (ContractListScreen)
- 显示所有监控的合约
- 按资金费率间隔排序
- 下拉刷新
- 点击查看详情

---

## 部署要求

### 环境要求
- Flutter SDK 3.19.0 或更高版本
- Dart 3.3.0 或更高版本
- Android Studio / VS Code
- Android SDK（Android 开发）
- Xcode（iOS 开发，仅 macOS）

### 运行方式
```bash
# 获取依赖
flutter pub get

# 运行开发版本
flutter run

# 构建 APK
flutter build apk --release

# 构建 iOS
flutter build ios --release
```

---

## 权限要求

### Android (android/app/src/main/AndroidManifest.xml)
```xml
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
```

### iOS (ios/Runner/Info.plist)
```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

---

## 待优化项

1. **通知服务扩展** - 可添加声音、振动等通知增强
2. **配置管理** - 支持自定义监控间隔、筛选条件
3. **错误处理** - 增强网络异常、API 错误处理
4. **UI/UX** - 添加图表展示资金费率趋势
5. **后台任务** - 支持应用在后台继续监控
6. **多语言** - 支持中英文切换
