---
phase: 07-rebound-test-page
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/services/test/test_orchestrator.dart
  - lib/services/test/test_data_generator.dart
  - test/services/test/test_orchestrator_test.dart
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
gap_closure: true
must_haves:
  truths:
    - "changeMode() 切换后，后续生成的 K 线数据特征符合新模式预期（如 steadyDecline 所有 close 递减）"
    - "changeMode() 保留原始 seed，保证同一 seed + 同一模式 = 同一数据序列"
  artifacts:
    - path: "lib/services/test/test_orchestrator.dart"
      provides: "修复后的 changeMode() 方法，替换 _generator 为新模式实例"
    - path: "lib/services/test/test_data_generator.dart"
      provides: "暴露 seed getter 供 TestOrchestrator 访问"
    - path: "test/services/test/test_orchestrator_test.dart"
      provides: "增强的 changeMode 测试，验证新模式数据特征"
  key_links:
    - from: "lib/services/test/test_orchestrator.dart"
      to: "lib/services/test/test_data_generator.dart"
      via: "changeMode() 创建新 TestDataGenerator(mode: mode, seed: _generator.seed)"
      pattern: "TestDataGenerator\\(mode:.*seed:"
---

<objective>
修复 TestOrchestrator.changeMode() 模式切换失效的阻断性 bug——方法接收 SimulationMode 参数但未实际使用，仅调用 _generator.reset() 而未创建新模式生成器。同时增强 changeMode 测试，验证切换后数据特征符合新模式预期。

Purpose: 07-VERIFICATION 发现的核心功能缺陷——ROADMAP 成功标准第 1 条要求"支持 4 种模式"，但当前 changeMode() 切换无效
Output: 修复后的 test_orchestrator.dart + test_data_generator.dart（seed getter）+ 增强的 test_orchestrator_test.dart
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/contract-quick-rebound/phases/07-rebound-test-page/07-VERIFICATION.md
@.planning/workstreams/contract-quick-rebound/phases/07-rebound-test-page/07-CONTEXT.md
@lib/services/test/test_orchestrator.dart
@lib/services/test/test_data_generator.dart
@test/services/test/test_orchestrator_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 changeMode() + 暴露 seed getter + 增强测试</name>
  <files>
    lib/services/test/test_data_generator.dart,
    lib/services/test/test_orchestrator.dart,
    test/services/test/test_orchestrator_test.dart
  </files>
  <read_first>
    lib/services/test/test_data_generator.dart,
    lib/services/test/test_orchestrator.dart,
    test/services/test/test_orchestrator_test.dart
  </read_first>
  <behavior>
    - TestDataGenerator 暴露 int? get seed => _seed 只读 getter
    - TestOrchestrator._generator 字段从 final 改为非 final（TestDataGenerator _generator）
    - TestOrchestrator.changeMode(SimulationMode mode) 内部创建新 TestDataGenerator(mode: mode, seed: _generator.seed) 替换 _generator
    - changeMode 后继续调用 _generator.nextCandle()，生成的数据特征符合新模式（如 steadyDecline 所有 close 严格递减）
    - changeMode 保留原始 seed：相同 seed + 相同模式 = 相同数据序列
    - 测试：changeMode 从 vRebound 切换到 steadyDecline 后，生成 30 根 K 线验证所有 close 递减
    - 测试：changeMode 切换回 vRebound 后，生成的数据符合 V 型反弹特征（第 20-22 根 close 递减）
    - 测试：changeMode 保留 seed 可重现性
  </behavior>
  <action>
    **修改 lib/services/test/test_data_generator.dart（per 07-VERIFICATION 修复方案）：**

    在 TestDataGenerator 类中添加 seed 只读 getter，供 TestOrchestrator.changeMode() 访问：
    - 在 `int get step => _step;` 下方添加 `int? get seed => _seed;`

    **修改 lib/services/test/test_orchestrator.dart（per 07-VERIFICATION BLOCKER 修复）：**

    1. 将 `_generator` 字段声明从 `final TestDataGenerator _generator` 改为 `TestDataGenerator _generator`（移除 final）
    2. 重写 `changeMode(SimulationMode mode)` 方法体：
       - 调用 `pause()`
       - 创建新生成器：`_generator = TestDataGenerator(mode: mode, seed: _generator.seed)`
       - 清空 `_window.clear()`
       - 清空 `_signals.clear()`
       - 重置 `_tickCount = 0`
       - 调用 `notifyListeners()`
       - 删除原有的 `_generator.reset()` 调用（reset 不切换模式）

    **修改 test/services/test/test_orchestrator_test.dart（per 07-VERIFICATION WARNING 修复）：**

    增强现有 `changeMode() 切换模式后 window 清空` 测试，新增以下测试用例：

    1. 将现有测试重命名为 `changeMode() 切换模式后 window 清空且暂停`
    2. 新增测试 `changeMode() 切换到 steadyDecline 后数据持续下跌`：
       - 创建 orchestrator（seed: 42）
       - 调用 changeMode(SimulationMode.steadyDecline)
       - 手动触发多次 tick（通过 start + 等待或直接访问内部逻辑）
       - 验证生成的 K 线 close 严格递减（每根 close < 前一根 close）
    3. 新增测试 `changeMode() 切换回 vRebound 后数据符合 V 型特征`：
       - 创建 orchestrator（seed: 42）
       - changeMode(SimulationMode.steadyDecline) 再 changeMode(SimulationMode.vRebound)
       - 生成 30 根 K 线，验证第 20-22 根 close 递减（急跌段）
    4. 新增测试 `changeMode() 保留 seed 可重现性`：
       - 创建两个 orchestrator（相同 seed）
       - 第一个 changeMode(steadyDecline) 生成 10 根
       - 第二个 changeMode(steadyDecline) 生成 10 根
       - 验证两者 window 数据完全一致

    由于 TestOrchestrator 的 _tick 是私有方法，测试策略：
    - 方案 A：通过 start() + Future.delayed(Duration(seconds: 55)) 等待足够 tick（慢）
    - 方案 B（推荐）：在 TestOrchestrator 上暴露一个 @visibleForTesting 的 tickForTest() 方法，直接触发 _tick 逻辑，避免真实 Timer 等待
    - 选择方案 B：在 test_orchestrator.dart 中添加 `@visibleForTesting void tickForTest() { _tickCount++; ... }` 或更简单地，在测试中通过 start() 后立即 pause() + 直接调用 generator.nextCandle() 来验证数据特征（不依赖 orchestrator 内部）

    实际推荐策略：直接测试 TestDataGenerator 在切换模式后的行为——
    - 创建 TestDataGenerator(mode: vRebound, seed: 42)，生成若干根
    - 创建 TestDataGenerator(mode: steadyDecline, seed: 42)，生成若干根
    - 验证 steadyDecline 的 close 全部递减
    - 同时在 orchestrator 测试中验证 changeMode 后 window 清空 + 调用 changeMode 后 isRunning 为 false

    最终测试文件需包含：
    - 原有 6 个测试保持不变（初始状态、start、pause、reset、changeMode 清空、dispose）
    - 新增 `changeMode() 切换到 steadyDecline 后 isRunning = false`
    - 新增 `changeMode() 后新生成器使用正确模式`：验证 changeMode 后调用 start() 生成的 window 数据特征
  </action>
  <verify>
    <automated>flutter test test/services/test/test_orchestrator_test.dart</automated>
  </verify>
  <done>
    changeMode() 实际切换 SimulationMode；TestDataGenerator 暴露 seed getter；测试验证切换后数据特征符合新模式；所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Service | TestOrchestrator 是本地服务，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-02 | — | — | — | 本阶段为纯本地 bug 修复，无安全影响 |
</threat_model>

<verification>
- `flutter test test/services/test/test_orchestrator_test.dart` 全部通过（含新增的 changeMode 数据特征测试）
- `flutter analyze lib/services/test/test_orchestrator.dart lib/services/test/test_data_generator.dart` 无 error
- changeMode(SimulationMode.steadyDecline) 后生成的 K 线 close 严格递减
- changeMode(SimulationMode.vRebound) 后生成的 K 线符合 V 型反弹周期特征
</verification>

<success_criteria>
1. TestOrchestrator.changeMode() 实际替换 _generator 为新模式实例
2. TestDataGenerator 暴露 seed getter
3. 测试覆盖 changeMode 后数据特征验证（不仅仅是 window 清空）
4. 所有现有测试 + 新增测试全部通过
</success_criteria>

<output>
Create `.planning/workstreams/contract-quick-rebound/phases/07-rebound-test-page/07-02-SUMMARY.md` when done
</output>
